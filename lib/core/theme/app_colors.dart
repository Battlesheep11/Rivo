import 'package:flutter/material.dart';

// Translating CSS variables to a Flutter theme
class AppColors {
  static const Color primary = Color(0xFF000000); // Black
  static const Color primaryLight = Color(0xFFF0F0F0); // Light gray
  static const Color secondary = Color(0xFF7EC4CF); // Soft teal
  static const Color text = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF666666);
  static const Color background = Color(0xFFF9FAFB);
  static const Color white = Color(0xFFFFFFFF);
  static const Color offWhite = Color(0xFFF8F9FA);
  static const Color border = Color(0xFFEAEAEA);
}
